name: cane_bluetooth_app
version: 1.0.0

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.0.0
  flutter_tts: ^3.2.7
  permission_handler: ^10.2.0
  provider: ^6.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
